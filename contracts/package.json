{
  "name": "polkaflow-contracts",
  "version": "1.0.0",
  "description": "PolkaFlow smart contracts",
  "scripts": {
    "compile": "hardhat compile",
    "test": "NODE_PATH=$(pwd)/node_modules hardhat test",
    "deploy:localhost": "hardhat ignition deploy --network localhost",
    "deploy:westend": "hardhat ignition deploy --network polkadot_hub_westend",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/node": "^20.0.0",
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.4.0",
    "ethers": "^6.11.0"
  }
}
